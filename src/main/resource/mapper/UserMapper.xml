<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserDao">
    <resultMap type="User" id="userResultMap">
        <id property="role_id" column="role_id"/>
        <result property="user_name" column="user_name"/>
        <result property="password" column="password"/>
        <result property="telephone" column="telephone"/>
        <result property="status" column="status"/>
        <result property="nickname" column="nickname"/>
        <result property="sex" column="sex"/>
        <result property="personal_sign" column="personal_sign"/>
        <result property="birthday" column="birthday"/>
    </resultMap>
    <insert id="addUser" parameterType="User">
--         INSERT INTO paper(paper_id,name,number,detail) VALUE (#{paperId},#{paperName}, #{paperNum}, #{paperDetail})
    </insert>

    <delete id="deleteUserById" parameterType="int">
         DELETE FROM user WHERE user_id=#{user_id}
    </delete>

    <update id="updateUser" parameterType="User">
        UPDATE user
        SET user_name = #{user_name},telephone = #{telephone},role_id = #{role_id}
        WHERE  user_id = #{user_id}
    </update>

    <select id="queryById" resultType="User" parameterType="int">
        SELECT user_id,user_name,telephone,role_id
        FROM user
        WHERE user_id=#{user_id}
    </select>
    <select id="queryAllPaper" resultMap="userResultMap">
        SELECT user_id, role_id, user_name, telephone, status
        FROM user
    </select>

</mapper>
